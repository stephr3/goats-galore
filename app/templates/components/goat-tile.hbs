{{#if inCart}}
<div class="sold-out">
  <h3>Sold Out!</h3>
</div>
{{/if}}
{{#link-to 'goat' goat.id}}
  <img src="{{goat.image}}" alt="picture of goat" />
{{/link-to}}
<h2>{{goat.name}}</h2>
<h5>{{goat.breed}} | ${{goat.price}}</h5>
